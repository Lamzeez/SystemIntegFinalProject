// @suggested_answers/debug_App_step3.tsx.txt
// This is the next debugging step for App.tsx

import 'react-native-gesture-handler'; // This must stay at the top
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { AuthProvider } from './src/AuthContext';
import { NavigationContainer } from '@react-navigation/native'; // Add this import

export default function App() {
  return (
    <AuthProvider>
      <NavigationContainer>
        <View style={styles.container}>
          <Text style={styles.text}>NavigationContainer is wrapping this.</Text>
          <Text style={styles.text}>Does it still render?</Text>
        </View>
      </NavigationContainer>
    </AuthProvider>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    // A new color to confirm the app has updated
    backgroundColor: 'lightgreen',
  },
  text: {
    fontSize: 24,
    color: 'black',
    fontWeight: 'bold',
    textAlign: 'center',
    margin: 10,
  },
});
