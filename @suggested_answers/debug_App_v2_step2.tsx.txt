// @suggested_answers/debug_App_v2_step2.tsx.txt
// Progressive Debugging Step 2

import 'react-native-gesture-handler';
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { AuthProvider, useAuth } from './src/AuthContext'; // Import useAuth
import { NavigationContainer } from '@react-navigation/native';

function AppNavigator() {
  // --- This is the line we are testing ---
  const { token, isLoading } = useAuth();

  return (
    <View style={styles.container}>
      <Text style={styles.text}>AppNavigator now uses useAuth().</Text>
      <Text style={styles.text}>isLoading: {isLoading ? 'true' : 'false'}</Text>
      <Text style={styles.text}>Token: {token ? 'Present' : 'null'}</Text>
    </View>
  );
}

export default function App() {
  return (
    <AuthProvider>
      <NavigationContainer>
        <AppNavigator />
      </NavigationContainer>
    </AuthProvider>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#008080', // "Teal" color to confirm update
  },
  text: {
    fontSize: 20,
    color: 'white',
    fontWeight: 'bold',
    textAlign: 'center',
    margin: 5,
  },
});
