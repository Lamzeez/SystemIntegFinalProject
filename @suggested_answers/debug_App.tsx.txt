// @suggested_answers/debug_App.tsx.txt
// This is a temporary file for debugging the blank screen issue.
// Please replace the contents of your App.tsx with this.

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>DOES THIS RENDER?</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    // Using a bright, obvious color
    backgroundColor: 'cyan',
  },
  text: {
    fontSize: 24,
    color: 'black',
    fontWeight: 'bold',
  },
});
