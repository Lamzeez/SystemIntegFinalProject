// @suggested_answers/debug_App_v2_step1.tsx.txt
// Progressive Debugging Step 1

import 'react-native-gesture-handler';
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { AuthProvider } from './src/AuthContext';
import { NavigationContainer } from '@react-navigation/native';

// This is the component we will build up.
// For now, it just returns a simple View.
function AppNavigator() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>This is the AppNavigator component.</Text>
    </View>
  );
}

export default function App() {
  return (
    <AuthProvider>
      <NavigationContainer>
        <AppNavigator />
      </NavigationContainer>
    </AuthProvider>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#6a5acd', // "SlateBlue"
  },
  text: {
    fontSize: 24,
    color: 'white',
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
